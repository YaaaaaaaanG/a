server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/java2007?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 123456

  jpa:
    database: MYSQL
    show-sql: true
    generate-ddl: true

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.qf.pojo
